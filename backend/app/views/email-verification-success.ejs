<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Email Verified Successfully - Social Media</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 500px;
      width: 100%;
      padding: 60px 40px;
      text-align: center;
      animation: slideUp 0.5s ease-out;
    }
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .success-icon {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px;
      animation: scaleIn 0.5s ease-out 0.2s both;
    }
    @keyframes scaleIn {
      from { transform: scale(0); }
      to { transform: scale(1); }
    }
    .success-icon svg {
      width: 60px;
      height: 60px;
      stroke: white;
      stroke-width: 3;
      stroke-linecap: round;
      stroke-linejoin: round;
      fill: none;
    }
    h1 {
      color: #262626;
      font-size: 32px;
      margin-bottom: 15px;
      font-weight: 700;
    }
    .message {
      color: #8e8e8e;
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 40px;
    }

    /* Confirmation Code Box */
    .otp-box {
      font-size: 28px;
      font-weight: bold;
      letter-spacing: 8px;
      padding: 15px 20px;
      background: #f0fdf4;
      color: #16a34a;
      border: 2px dashed #16a34a;
      border-radius: 10px;
      display: inline-block;
      margin-bottom: 30px;
      animation: fadeIn 0.6s ease-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .button-container {
      display: flex;
      gap: 15px;
      justify-content: center;
      flex-wrap: wrap;
    }
    .button {
      padding: 14px 32px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      font-size: 16px;
      transition: all 0.3s ease;
      cursor: pointer;
      border: none;
    }
    .button-primary {
      background: linear-gradient(135deg, #405DE6 0%, #5B51D8 100%);
      color: white;
    }
    .button-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(64, 93, 230, 0.3);
    }
    .button-secondary {
      background: #f8f9fa;
      color: #262626;
      border: 2px solid #e9ecef;
    }
    .button-secondary:hover {
      background: #e9ecef;
      transform: translateY(-2px);
    }

    .error-container {
      background: #fee2e2;
      border-left: 4px solid #ef4444;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 20px;
    }
    .error-container h2 {
      color: #dc2626;
      font-size: 20px;
      margin-bottom: 10px;
    }
    .error-container p {
      color: #991b1b;
      font-size: 14px;
    }
  </style>
</head>

<body>
  <div class="container">
    <% if (success) { %>

      <div class="success-icon">
        <svg viewBox="0 0 24 24">
          <polyline points="20 6 9 17 4 12"></polyline>
        </svg>
      </div>

      <h1>Email Verified Successfully!</h1>

      <p class="message">
        Your email address has been verified. Below is your confirmation code.
      </p>

      <!-- ⭐ CONFIRMATION CODE BOX ⭐ -->
      <div class="otp-box">
        <%= otp %>
      </div>

      <div class="button-container">
        <a href="<%= baseUrl || 'http://localhost:5004' %>/api-docs" class="button button-primary">
          View API Documentation
        </a>
        <a href="<%= baseUrl || 'http://localhost:5004' %>" class="button button-secondary">
          Go to Home
        </a>
      </div>

    <% } else { %>

      <div class="error-container">
        <h2>Verification Failed</h2>
        <p><%= error || 'This confirmation link is invalid or expired. Please request a new one.' %></p>
      </div>

      <div class="button-container">
        <a href="<%= baseUrl || 'http://localhost:5004' %>/api-docs" class="button button-primary">
          View API Documentation
        </a>
        <a href="<%= baseUrl || 'http://localhost:5004' %>" class="button button-secondary">
          Go to Home
        </a>
      </div>

    <% } %>
  </div>
</body>
</html>
